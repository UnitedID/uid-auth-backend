//import org.unitedid.auth.backend.Password

class BootStrap {

    def init = { servletContext ->
        /*def cred = new CredentialStore()
        cred.credential = new Password(status: "active", version: 1)
        cred.save(flush: true)*/
    }
    def destroy = {
    }
}
